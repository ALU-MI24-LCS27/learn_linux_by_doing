#!/bin/bash

# Navigate to the directory where your CSV file is
cd /path/to/data

# Sort the CSV file by the second column (temperature) and extract the top 5 lowest temperatures
# Assuming column 1 is the month and column 2 is the temperature
# Skip the header (tail -n +2) and sort the second column, then take the top 5 lowest entries
cat satelite_temperature_data.csv | tail -n +2 "$input_file" | sort -t ',' -k3 | head -n 5 > top-5-lowest-temperatures.csv

# Add the header back to the output file
sed -i '1iMonth,Temperature' top-5-lowest-temperatures.csv

# Output the contents of the resulting file
cat top-5-lowest-temperatures.csv

